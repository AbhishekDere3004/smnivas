// tailwind.config.js

module.exports = {
  content: ["./src/**/*.{html,js,jsx,ts,tsx}"], // Specify the content to process for Tailwind CSS
  theme: {
    extend: {}, // Extend the theme if needed
  },
  plugins: [], // Add plugins if needed
};
